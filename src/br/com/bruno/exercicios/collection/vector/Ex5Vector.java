package br.com.bruno.exercicios.collection.vector;

import java.util.Arrays;
import java.util.Vector;

public class Ex5Vector {
    public static void main(String[] args) {
        // Invertendo a ordem dos elementos de um vetor: Crie um vetor de números reais com 7 elementos, inicialize-os com valores e inverta a ordem dos elementos usando um for.
        Vector<Integer> v = new Vector<Integer>(Arrays.asList(1, 2, 3, 4));

        for(int x = (v.size() - 1); x >= 0; x--){

        }
    }
}
